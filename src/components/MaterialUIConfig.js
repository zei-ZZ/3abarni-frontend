// MaterialUIConfig.js
import { createTheme } from '@mui/material/styles';

// Define your theme
export const theme = createTheme({
  // Your theme configurations go here
});

// Export any Material-UI components you might use globally
export { Modal, Box, Typography, Button } from '@mui/material';
